# Nameless Audio 0.15.2 Release Notes

> **升级前必读：**从不兼容的旧版本升级到本 Release 时，必须先卸载旧版本再重新安装。卸载会清除应用数据，建议先在“数据支持”中导出 `.nalbackup` 备份。具体兼容范围以本次发布说明为准。

## 播放、加载与字幕

- ASMR.ONE 音频在开始播放或缓冲期间，播放列表卡片与播放详情页的播放按钮会显示转圈图标，字幕区域同步显示“加载中”；点击转圈按钮仍可切换播放状态。
- 快退、快进和拖动进度后增加短暂加载阈值，瞬时缓冲不再触发加载界面，持续等待时才显示明确反馈。
- 播放详情页的加载字幕新增淡入淡出，封面尺寸变化改为平滑过渡；简洁字幕与时间轴字幕继续复用统一的切换效果。
- 时间轴字幕支持按内容动态调整条目高度，并优化了定位、跳转和滚动体验。
- 完善播放完成、错误和恢复状态处理，避免终止状态下残留错误播放意图。

## 设置、备份与本地化

- 备份导出前会完整落盘所有可持久化的设置页选项，确保主题、语言、播放、字幕、ASMR.ONE 等设置随 `.nalbackup` 一并保存；备份同时包含 ASMR.ONE 用户名、密码和 JWT/token 的明文副本，ZIP 本身不加密，恢复备份会恢复这些凭据，请谨慎保管和分享。
- 对齐并优化中文、日文和英文文案，详情页标题和下载同名文件处理用语更简洁；各语言的音频详情标题不再包含“音声”字样。
- 修复英文等较长文案在窄屏和大字体下的下拉框溢出，同时保留展开菜单的可读间距。

## 界面与媒体库

- 优化页面标题栏收起时标题、说明文字和操作按钮的渐变、位移与交互禁用效果。
- 媒体库首次加载期间保留骨架卡片，减少内容尚未就绪时的空白跳变。
- 复用目录封面查询并延后交互期间的文件读取；作品目录没有直接图片时可继续查找嵌套图片。
- 整理播放列表状态与启动副作用，减少构建阶段的重复工作。

## 稳定性与发布验证

- 加强 Android 播放服务、错误本地化和恢复链路，并补充发布签名、设备冒烟测试及 Release 资产校验。
- 修复设置页多语言响应式测试发现的窄屏布局回归。

## 发布资产

```text
NamelessAudio-android-universal-<tag>.apk
NamelessAudio-android-universal-<tag>.apk.sha256
NamelessAudio-android-arm64-<tag>.apk
NamelessAudio-android-arm64-<tag>.apk.sha256
NamelessAudio-android-armv7-<tag>.apk
NamelessAudio-android-armv7-<tag>.apk.sha256
NamelessAudio-android-x64-<tag>.apk
NamelessAudio-android-x64-<tag>.apk.sha256
```

普通 Android 用户可下载 universal APK。现代手机可选择 arm64，旧款 32 位 ARM 设备选择 armv7，x86_64 仅用于对应设备或模拟器。
