org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Keep disabled while Flutter plugin sources can live on a different Windows drive
# from the app project; Kotlin's incremental cache path conversion is not
# cross-drive safe in that setup.
kotlin.incremental=false
