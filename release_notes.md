# Doujin Audio 0.19.0 Release Notes

> **升级前必读：**本版本使用 Android 应用 ID `com.doujin.audio`，会作为独立应用安装，不能覆盖更名前版本，也不会继承旧应用私有数据。应用仅接受由本版本创建的 `.dabackup` 备份，不提供旧数据或旧备份格式的自动迁移。

## 播放与界面

- **播放会话批量操作**：在播放列表长按会话即可多选，并批量播放、暂停或移除；批量播放会遵循多会话播放设置。
- **音频焦点策略**：Android 新增“标准”和“与其他应用混音”两种策略；混音模式允许 Doujin Audio 与其他应用同时发声，并会持久保存选择。
- **字幕时间轴吸附**：手动滚动时间轴字幕后会吸附到最近字幕，浏览期间保持当前位置，停止操作后自动回到当前播放字幕，也可直接跳转到选中时间点。
- **竖屏控制台细节**：优化竖屏播放详情的时间段控件、操作布局和音频详情动作，减少拥挤与误触。

## 本地媒体库

- **文件夹展开与搜索优化**：展开的文件夹在刷新子项时保持可见，大型媒体库的展开、搜索和列表更新按可见范围处理，降低无关重建和卡顿。
- **移除流程统一**：媒体库移除、排除与恢复共用同一状态路径，避免刷新后目录意外回到列表或状态不一致。
- **状态写入保护**：主题、字幕、媒体库快照和设置写入失败时不再提交过期状态，降低快速连续操作造成的回退或覆盖风险。

## ASMR.ONE 下载与数据可靠性

- **下载暂停与续传增强**：应用进入后台或退出时会先安全暂停全部任务；暂停或失败任务可重新入队，并复用经过响应校验的临时文件继续下载。
- **下载目录授权保留**：持久保存已选择下载目录的 Android SAF 授权，应用重启后无需重复选择目录。
- **分类与作品加载优化**：作品分类和文件树使用稳定的加载、失败重试与空状态展示，作品详情可直接进入下载页面，推荐计算与大型列表更新减少主线程工作。
- **账号与作品数据原子化**：ASMR.ONE 账号同步改为原子持久化，并清理不再被收藏、历史或下载引用的孤立作品记录。
- **备份与恢复加固**：`.dabackup` 改进流式读写、内容边界校验和原子替换，异常时回滚原数据，减少大型备份的内存峰值与半写入风险。
- **竞态修复**：修复下载响应校验、扫描清理、睡眠计时器恢复以及快速设置写入中的竞态条件。

## 发布资产

```text
DoujinAudio-android-universal-<tag>.apk
DoujinAudio-android-universal-<tag>.apk.sha256
DoujinAudio-android-arm64-<tag>.apk
DoujinAudio-android-arm64-<tag>.apk.sha256
DoujinAudio-android-armv7-<tag>.apk
DoujinAudio-android-armv7-<tag>.apk.sha256
DoujinAudio-android-x64-<tag>.apk
DoujinAudio-android-x64-<tag>.apk.sha256
```

普通 Android 用户可下载 universal APK。现代手机可选择 arm64，旧款 32 位 ARM 设备选择 armv7，x86_64 仅用于对应设备或模拟器。所有 APK 都附带同名 `.sha256`，并由 GitHub Actions 校验正式签名和 ABI 后发布。
