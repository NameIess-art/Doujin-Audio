# Doujin Audio 0.21.0 Release Notes

> **升级前必读：**本版本使用 Android 应用 ID `com.doujin.audio`，会作为独立应用安装，不能覆盖更名前版本，也不会继承旧应用私有数据。应用仅接受由本版本创建的 `.dabackup` 备份，不提供旧数据或旧备份格式的自动迁移。

## DLsite 元数据批量抓取与审查

- **批量抓取选择器与浮动顶栏对齐**：统一媒体库批量抓取页面的视觉与顶栏交互，布局层次更清晰。
- **批量作品审查导航**：在 DLsite 元数据审查页面新增上一部/下一部作品快速切换导航，批量核对、编辑与保存更便捷。

## 本地媒体库与搜索

- **媒体库搜索批量多选**：媒体库搜索支持长按进入批量选择模式，支持多选添加至播放列表或集中管理。
- **可撤销的移除过渡**：从媒体库中移除目录或作品时提供平滑的撤销提示与过渡动效，误触可一键撤销恢复。
- **骨架屏与紧凑卡片布局对齐**：细化媒体库骨架屏占位元素与真实卡片操作按钮位置，消除数据加载完成时的视觉跳动。
- **树结构状态与折叠恢复**：优化目录树层级展开与清空搜索时的折叠回退逻辑，提升浏览流畅度。

## 播放控制与队列逻辑加固

- **队列移除曲目持续播放**：从播放队列中移除当前正在播放的音轨时，保持当前曲目正常播放直至自然播完或手动切歌，不中断收听体验。
- **跨文件夹循环与队列索引计算**：修复循环范围与播放队列在特定层级跳转时的索引偏移问题。
- **多会话管理与过渡动效**：优化播放会话卡片排序、吸顶标题与手势交互，加固意外中断后的会话状态恢复。

## ASMR.ONE 与下载管理

- **作品卡片手势操作优化**：优化 ASMR 作品卡片的滑动交互响应与按钮层级，操作更加跟手。
- **下载传输与断点续传重构**：重构底层下载传输与重试协调逻辑，面对弱网与瞬时服务端错误时具备更稳定的指数退避与自动续传能力。
- **回顶动画与标题栏状态修复**：优化长列表一键回到顶部的动效平滑度与顶栏高度自适应。

## 设置与系统功能

- **权限设置内联控件组**：将独立的权限说明页面重构为设置页中的内联状态控件组，实时查看与配置存储及通知权限。
- **设置分类粘性分组标题**：设置页面引入粘性分类标题（Sticky Section Headers），长页面浏览定位更轻松。
- **主题实时切换重绘**：通过 ThemeProvider 统一响应主题颜色与模式切换，界面元素即时更新无需刷新。
- **视频转音频输出路径记忆**：持久化记录视频转音频的输出目录偏好，省去每次转换时的重复选择。

## 发布资产

```text
DoujinAudio-android-universal-<tag>.apk
DoujinAudio-android-universal-<tag>.apk.sha256
DoujinAudio-android-arm64-<tag>.apk
DoujinAudio-android-arm64-<tag>.apk.sha256
DoujinAudio-android-armv7-<tag>.apk
DoujinAudio-android-armv7-<tag>.apk.sha256
DoujinAudio-android-x64-<tag>.apk
DoujinAudio-android-x64-<tag>.apk.sha256
```

普通 Android 用户可下载 universal APK。现代手机可选择 arm64，旧款 32 位 ARM 设备选择 armv7，x86_64 仅用于对应设备或模拟器。所有 APK 都附带同名 `.sha256`，并由 GitHub Actions 校验正式签名和 ABI 后发布。
